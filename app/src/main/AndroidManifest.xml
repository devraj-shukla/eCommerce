<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecommerce">
    <!-- Include required permissions for Google Mobile Ads to run. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerce">
        <activity android:name=".activity.Order"></activity>
<!--        <activity android:name=".activity.RecyclerProductListing" />-->
        <activity android:name=".activity.ProductListing" />
<!--        <activity android:name=".activity.RecyclerSubCategoryAdaptor" />-->
        <activity android:name=".activity.SubCategory">
        </activity>
<!--        <activity android:name=".activity.RecyclerHomePageAdaptor2" />-->
<!--        <activity android:name=".activity.RecyclerCartAdaptor" />-->
        <activity android:name=".activity.Cart" >

        </activity>
        <activity android:name=".activity.ZoomedImage" />
        <activity android:name=".activity.ProductDetails" />
        <activity android:name=".activity.HomePageDetails">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>